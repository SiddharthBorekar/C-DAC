const myName = "Siddharth borekar";

const oddNumber =(num)=>{
    if (num%2!=0) {
        return `${num} is odd number`;
    }else{
        return `${num} is odd number`;
    }
    return `${num} is even Number`;
}
module.exports = {myName,oddNumber}